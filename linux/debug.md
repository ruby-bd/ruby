## Ruby Mine Ruby Debugging

```sh
sudo gem install rubocop
sudo gem install rake
sudo gem install debase
sudo gem install rdebug
sudo gem install ruby-debug-ide
sudo gem install ruby-debug-ide --pre
```
